import styled from 'styled-components';
import { motion } from 'framer-motion';

export const CardContainer = styled(motion.div)`
  width: fit-content;
  // light red pastel color for background
  background: #ffcccc;
  border-radius: 10px;
`;
